apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-ai-service-to-openai
  namespace: carenest-dev
spec:
  podSelector:
    matchLabels:
      app: ai-service
  policyTypes:
  - Egress
  egress:
  - ports:
    - port: 443
      protocol: TCP
